type: object
required:
  - message
properties:
  message:
    type: string
    description: Free-form human-readable log message.
  timestamp:
    type: string
    format: date-time
    description: RFC3339 timestamp added by fluxid; ignored if supplied by client.
additionalProperties: false